# {{.Recipe.Name}}

This is a customer-owned feature-flag service generated by SaaS Reaper.
It contains the application, selected persistence mechanism, deployment pack,
domain record, and agent instructions. It does not call home to SaaS Reaper.

## Selected stack

- Service language: `{{.Recipe.Service.Language}}`
- Database authority: `{{.Recipe.Database.Authority}}`
- Deployment: `{{.Recipe.Deployment.Target}}`
- Replicas: `{{.Recipe.Deployment.Replicas}}`
- Tenant concept: `{{.Recipe.Domain.Tenant}}`

`REAPER.yaml` is the normalized customer recipe. `REAPER.lock.yaml` records the
factory version that produced this repository.

## Run locally

```sh
make setup
make demo
```

The demo starts the service, publishes a boolean flag, evaluates a targeting
match over the OFREP-compatible HTTP surface, and stops the service.

Run the repository checks:

```sh
make check
```

## Architecture

```text
management HTTP ──┐
                  ▼
                flags ◄──── store/{{.Recipe.Database.Authority}}
                  ▲
evaluation HTTP ──┘
```

Flag validation and evaluation own policy. HTTP and database packages own
mechanism. The composition root is the only place concrete mechanisms meet.

## Deployment

The selected deployment material is under `deploy/{{.Recipe.Deployment.Target}}`.
It is source material for the customer to review and apply; generation never
provisions external infrastructure.

## Modification

Read `AGENTS.md`, `DOMAIN.md`, and the relevant recipe under `skills/` before
changing domain attributes, evaluation behavior, storage, or deployment.
