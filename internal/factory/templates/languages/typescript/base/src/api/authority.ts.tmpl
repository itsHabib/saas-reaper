import type { Flag } from "../flags/flag.ts";

export interface FlagAuthority {
  initialize(): Promise<void>;
  load(environment: string, key: string): Promise<Flag | undefined>;
  publish(environment: string, flag: Flag, expectedRevision: number, actor: string): Promise<Flag>;
  close(): Promise<void>;
}
