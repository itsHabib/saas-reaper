FROM node:24-alpine

WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev
COPY src ./src
RUN mkdir -p /data && chown node:node /data

ENV PORT=8080
EXPOSE 8080
USER node
CMD ["node", "src/main.ts"]
