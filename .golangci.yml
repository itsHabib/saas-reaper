version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - cyclop
    - gocognit
    - nestif
    - revive
  settings:
    cyclop:
      max-complexity: 15
    gocognit:
      min-complexity: 20
    nestif:
      min-complexity: 4
