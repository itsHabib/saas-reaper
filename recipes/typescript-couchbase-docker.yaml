schema: reaper.dev/v0alpha2
name: team-flags
capability: feature-flags
service:
  language: typescript
database:
  authority: couchbase
deployment:
  target: docker
  replicas: 1
delivery:
  format: directory
domain:
  tenant: organization
  targetingAttributes:
    - targetingKey
    - organization.id
