schema: reaper.dev/v0alpha2
name: checkout-flags
capability: feature-flags
service:
  language: typescript
database:
  authority: sqlite
deployment:
  target: docker
  replicas: 1
delivery:
  format: both
domain:
  tenant: organization
  targetingAttributes:
    - targetingKey
    - organization.id
