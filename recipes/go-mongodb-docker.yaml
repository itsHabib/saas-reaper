schema: reaper.dev/v0alpha2
name: go-mongodb-flags
capability: feature-flags
service:
  language: go
database:
  authority: mongodb
deployment:
  target: docker
  replicas: 1
delivery:
  format: directory
domain:
  tenant: organization
  targetingAttributes:
    - targetingKey
    - organization.id
